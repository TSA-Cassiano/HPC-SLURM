# this is for me. I have short memory

working on a branch:

main_user   = TSA-Cassiano
repo_name   = HPC-SLURM
branch_name = main

git clone git@github.com:main_user/repo_name
cd repo_name/
git remote -v
git remote add main_user  git://github.com/main_user/repo_name
git fetch origin
git checkout -b dev origin/branch_name

to add changes:
git add . (or git add -A)
git commit -m "some text"
git push origin branch_name


Someonelse did changes:
git pull main_user master
git push


## 
git reset --hard
git pull
